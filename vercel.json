{
  "build": {
    "env": {
      "JEKYLL_ENV": "production"
    }
  },
  "builds": [
    {
      "src": "_config.yml",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "_site"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}